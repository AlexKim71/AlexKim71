name: Generate Pac-Man Graph

on:
  schedule:
    - cron: "0 2 * * *"  # каждый день в 2:00 UTC
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 5

    steps:
      - name: 📥 Checkout репозитория
        uses: actions/checkout@v4

      - name: 🧹 Очистить dist/
        run: |
          rm -rf dist
          mkdir -p dist

      - name: 🎮 Генерация pacman-contribution-graph.svg
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: ${{ github.repository_owner }}

      - name: ✅ Проверка наличия SVG
        run: |
          if [ ! -f dist/pacman-contribution-graph.svg ]; then
            echo "❌ SVG не найден. Ошибка генерации."
            exit 1
          fi

      - name: 🚀 Push в ветку output
        uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
